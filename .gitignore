node_modules
.env
server/.env
client/.env
data
.vite
dist
build

# ─────────────────────────────────────────
# OS / Editor junk
# ─────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*.swo
*.orig
*.tmp
*.bak
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
*.iml

# ─────────────────────────────────────────
# Node & package managers
# ─────────────────────────────────────────
node_modules/
client/node_modules/
server/node_modules/
.pnpm-store/
.npm/
.yarn/*
!.yarn/releases
!.yarn/plugins
!.yarn/versions

# Keep lockfiles! (good for reproducible installs)
# !package-lock.json
# !client/package-lock.json
# !server/package-lock.json
# !pnpm-lock.yaml
# !yarn.lock

# ─────────────────────────────────────────
# Build artifacts / caches
# ─────────────────────────────────────────
dist/
client/dist/
server/dist/
build/
client/build/
server/build/
.tmp/
temp/
tmp/

# Vite / tooling caches
client/.vite/
client/.cache/
server/.cache/
.eslintcache
.stylelintcache
.turbo/
.parcel-cache/

# ─────────────────────────────────────────
# Environment & secrets
# (commit .env.example instead)
# ─────────────────────────────────────────
.env
.env.*
client/.env
client/.env.*
server/.env
server/.env.*

# Local SSL/dev certs (keep a README if needed)
certs/
server/certs/
*.pem
*.crt
*.key
*.cert

# ─────────────────────────────────────────
# SQLite databases & related files
# ─────────────────────────────────────────
*.db
*.db-shm
*.db-wal
shop.db
server/shop.db
server/shop.db-shm
server/shop.db-wal

# ─────────────────────────────────────────
# Uploads / backups / exports (optional)
# ─────────────────────────────────────────
uploads/
server/uploads/
backup/
backups/
*.backup.json
*backup*.json

# ─────────────────────────────────────────
# Coverage / reports
# ─────────────────────────────────────────
coverage/
reports/
*.lcov

# ─────────────────────────────────────────
# Logs at runtime
# ─────────────────────────────────────────
logs/
*.log*
server/logs/
client/logs/

# ─────────────────────────────────────────
# Deployment / misc
# ─────────────────────────────────────────
# Ignore local nginx snippets, keep versioned ones under deploy/
nginx/*.local.conf
*.local.nginx.conf

# ─────────────────────────────────────────
# Markdown
# ─────────────────────────────────────────
*.md        # ignore markdown files (kept locally)
!README.md  # but keep README.md
!readme.md  # and keep readme.md (lowercase)
!docs.md    # and keep docs.md

# Notes (kept untracked)
nips.md
Note.md
bufferize.md